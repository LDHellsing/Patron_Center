@model Patron_Center.Models.CursoUsuario

@{
    ViewData["Title"] = "Create";
}

<h1>Incripción de Alumnos</h1>

<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="UsuarioId" class="control-label"></label>
                <select asp-for="UsuarioId" class="form-control" asp-items="ViewBag.UsuarioId"></select>
            </div>
            <div class="form-group">
                <label asp-for="CursoId" class="control-label"></label>
                <select asp-for="CursoId" class="form-control" asp-items="ViewBag.CursoId"></select>
            </div>
            @if (ViewBag.UsuarioYaEnCurso != null)
            {
                <span class="text-danger">@ViewBag.UsuarioYaEnCurso</span>
            }
            <div class="form-group">
                <input type="submit" value="Inscribir" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index" asp-route-CursoId=@ViewBag.CursoId_>Volver a la lista</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
