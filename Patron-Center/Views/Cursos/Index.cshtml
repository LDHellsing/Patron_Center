@model IEnumerable<Patron_Center.Models.Curso>

@{
    ViewData["Title"] = "Index";
}

<h1>Listado de Cursos</h1>
@if (ViewBag.TipoUsuario == "Administrador")
{
    <p>
        <a asp-action="Create">Crear</a>
    </p>
}
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nombre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Descripcion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FechaFinalizacion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Eliminado)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Docente)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Nombre)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Descripcion)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.FechaFinalizacion)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Eliminado)
                </td>
                <td>
                    @(String.Format("{0} {1}", item.Docente.Nombre, item.Docente.Apellido))
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-link">Editar</a>
                    <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-link">Detalles</a>
                    <a asp-controller="Unidades" asp-action="Index" asp-route-CursoId="@item.Id" class="btn btn-link">Unidades</a>
                </td>
            </tr>
        }
    </tbody>
</table>
