@model Patron_Center.Models.RespuestaAlumnoMO

@{
    ViewData["Title"] = "Anawer Quiz";
}

<h1>@Model.QuizName</h1>
<br />


<div class="row">
    <form asp-action="QuizCorrection" method="post">
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
        <div class="form-group">

            @for (var i = 0; i < @Model.Preguntas.Count(); i++)
            {
                <div class="form-group">
                    <b>
                        @Html.HiddenFor(x => Model.Preguntas[i].IdPregunta)
                        @Html.LabelFor(m => @Model.Preguntas[i].IdPregunta, @Model.Preguntas[i].Enunciado)
                    </b>
                </div>
                @for (var j = 0; j < @Model.Preguntas[i].Respuestas.Count(); j++)
                {
                    @Html.HiddenFor(x => Model.Preguntas[i].Respuestas[j].IdRespuesta)
                    <div class="form-group ml-3" >
                        @Html.RadioButtonFor(x => x.Preguntas[i].Seleccionada, Model.Preguntas[i].Respuestas[j].IdRespuesta) @Model.Preguntas[i].Respuestas[j].Enunciado
                    </div>
                }
            }





            @*@foreach (var pregunta in @Model.Preguntas)
                {
                <div>
                    <label asp-for="@pregunta.Enunciado" class="control-label">@pregunta.Enunciado</label>
                </div>
                <div class="form-group">
                    <input asp-for="@pregunta.IdPregunta" class="form-control data-form" hidden="hidden" />
                </div>
                <div class="form-group">
                    @foreach (var respuesta in pregunta.Respuestas)
                    {
                    <div class="form-group">
                        <input asp-for="@respuesta.IdRespuesta" class="form-control data-form" hidden="hidden" />
                    </div>
                    }
                </div>
                }*@

        </div>
        <div class="form-group">
            <input asp-for="@Model.IdQuiz" class="form-control data-form" hidden="hidden" />
        </div>
        <div class="form-group">
            <input type="submit" value="Enviar" class="btn btn-primary" />
        </div>
    </form>

</div>